from setuptools import setup, find_packages

setup(name='fireMoniteringProject', version='1.0', packages=find_packages())


# Run the below command in terminal of current directory
# pip install -e .
